version: "3.9"

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    restart: always
    ports:
      - "27018:27017"
    environment:
      MONGO_INITDB_DATABASE: e_commerce
    # volumes:
    #   - ./data/db:/data/db
#! open on deploy
# app:
#   build:
#     context: .
#   container_name: e_commerce_app
#   restart: always
#   ports:
#     - "8000:8000"
#   depends_on:
#     - mongodb
#   command: npm start
